import { RegisterUserModel } from './register-user-model';

describe('RegisterUserModel', () => {
  it('should create an instance', () => {
    expect(new RegisterUserModel()).toBeTruthy();
  });
});
