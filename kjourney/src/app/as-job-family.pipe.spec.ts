import { AsJobFamilyPipe } from './as-job-family.pipe';

describe('AsJobFamilyPipe', () => {
  it('create an instance', () => {
    const pipe = new AsJobFamilyPipe();
    expect(pipe).toBeTruthy();
  });
});
